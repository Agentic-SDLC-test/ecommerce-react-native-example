# Required — copy this file to .env and set the URL for your platform.
# Restart Expo after changing (npm start / npm run web).
EXPO_PUBLIC_API_URL=http://localhost:3002
# Enable the mock wallet checkout option only when the selected backend
# supports the enriched /checkout and /orders payment contract.
EXPO_PUBLIC_ENABLE_MOCK_WALLET_PAYMENT=true
